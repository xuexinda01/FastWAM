# @package _global_

# dataloading
batch_size: 1
num_workers: 4

model:
  mot_checkpoint_mixed_attn: false

# scheduler
lr_scheduler_type: "cosine"
learning_rate: 1e-4
num_epochs: 1
max_steps: null
log_every: 10
save_every: 100
eval_every: 10

# training
gradient_accumulation_steps: 1
weight_decay: 1e-2
resume: "/apdcephfs_tj5/share_302528826/xxd/nav_vln_1e-4/2026-05-19_01-29-02/checkpoints/weights/step_002800.pt" 
